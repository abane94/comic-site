export const collectionSuffix = 'Col';

export const bookCollection = 'book' + collectionSuffix;
export const seriesCollection = 'series' + collectionSuffix;
export const userCollection = 'user' + collectionSuffix;

export const collections = [
    bookCollection,
    seriesCollection,
    userCollection
];